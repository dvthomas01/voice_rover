# Serial communication
pyserial>=3.5

# Audio processing
numpy>=1.24.0
sounddevice  # Primary audio capture (16kHz float32, Whisper-ready)

# Wake word detection
pvporcupine>=3.0.0
# Note: Requires Porcupine access key (set PORCUPINE_ACCESS_KEY env var)
# Get key from: https://console.picovoice.ai/

# Speech-to-text
openai-whisper>=20230314
torch>=2.0.0
# Note: Whisper models are downloaded automatically on first use
# Model sizes: tiny, base, small, medium, large (base recommended for speed)

# Testing
pytest>=7.4.0
pytest-mock>=3.11.1
